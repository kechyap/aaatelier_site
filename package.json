{
  "name": "aaatelier_site",
  "version": "1.0.1",
  "private": true,
  "homepage": "https://ejay.kim/",
  "dependencies": {
    "gh-pages": "^2.1.1",
    "normalize.css": "^8.0.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-flexbox-grid": "^2.1.2",
    "react-router-dom": "^5.0.1",
    "react-scripts": "^3.1.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom",
    "build": "react-scripts build",
    "predeploy": "yarn build && cp ./public/CNAME ./build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
